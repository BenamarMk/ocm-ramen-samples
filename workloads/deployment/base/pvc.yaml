---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: busybox-pvc
spec:
  accessModes:
    - ReadWriteOnce
  # Customize in your overlay.
  storageClassName: STORAGE_CLASS_NAME
  volumeMode: Block
  resources:
    requests:
      storage: 1Gi
