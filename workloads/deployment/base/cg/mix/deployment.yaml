---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: busybox
spec:
  replicas: 1
  selector:
    matchLabels:
      appname: busybox
  template:
    metadata:
      labels:
        appname: busybox
    spec:
      containers:
        - name: logger
          image: quay.io/nirsof/busybox:stable
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              emit() {
                  echo "$(date) $1" | tee -a /var/log/ramen.log /var/log2/ramen.log /var/logrbd/ramen.log /var/logrbd2/ramen.log
                  sync
              }
              trap "emit STOP; exit" TERM
              emit START
              while true; do
                  sleep 10 & wait
                  emit UPDATE
              done
          volumeMounts:
            - name: varlog
              mountPath: /var/log
            - name: varlog2
              mountPath: /var/log2
            - name: varlogrbd
              mountPath: /var/logrbd
            - name: varlogrbd2
              mountPath: /var/logrbd2
      volumes:
        - name: varlog
          persistentVolumeClaim:
            claimName: busybox-cg1-pvc1
        - name: varlog2
          persistentVolumeClaim:
            claimName:  busybox-cg1-pvc2
        - name: varlogrbd
          persistentVolumeClaim:
            claimName: busybox-cg2-pvc1-rbd
        - name: varlogrbd2
          persistentVolumeClaim:
            claimName:  busybox-cg2-pvc2-rbd